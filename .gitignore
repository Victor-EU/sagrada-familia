node_modules
dist
.DS_Store

# Reference photographs are dev-harness input only and are never shipped.
# The note explaining the directory, and the record of where the photographs
# came from, stay tracked.
reference/*
!reference/README.md
!reference/SOURCES.md

# Frame grabs from the dev harness.
reference/.shots

# Victor's own photographs of the building — half a gigabyte of raw JPEG, and
# the single best reference this project has. Kept out of the history for the
# same reason reference/ is: they are harness input, not source.
Photos/
